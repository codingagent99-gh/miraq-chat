# Separate virtualenv for translation_service/ ONLY — not the chat backend.
# For a CPU-only box, install torch from the CPU index first to avoid the
# multi-GB CUDA wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
transformers
sentencepiece
IndicTransToolkit
flask
gunicorn
